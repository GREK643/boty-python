

#sell order
sell = market_order_sell()

#buy order
buy = market_order_buy()

#returns the entire dictionary with the balance of each cryptocurrency.
get_balance()

#returns the amount of cryptocurrency in the wallet (balance.json file) e.g get_symbol_balance('BTC')
get_symbol_balance(symbol)

#returns a dictionary with information about transactions (plik transactions.json)
get_transactions()